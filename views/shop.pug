extends layouts/main-layout.pug

block styles

block content
    main
        p List of products:
            if prods.length>0
                each product in prods
                    ul
                        li #{product.title}
            else
                h1 No Products!
